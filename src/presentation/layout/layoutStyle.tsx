import {StyleSheet} from 'react-native'

export const layoutStyle = StyleSheet.create({
    container:{
        paddingHorizontal:10,
        backgroundColor:'#fff'
    },
    header:{
        paddingHorizontal:0, 
    }
})